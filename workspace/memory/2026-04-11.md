# 2026-04-11 Daily Notes

## 00:00–01:00 AM — idle

## 02:00–02:06 AM — Scan hosting vi phạm (cron tự động)

- Cron scan 4 server: 51.222.11.232, 36.50.135.145, 160.191.244.160, 160.191.244.99
- Kết quả: 3/4 server quét xong, ~350 account vi phạm phát hiện
- 160.191.244.99 bị timeout chưa hoàn thành (lần thứ 6)
- Vi phạm nổi bật: fake bill ngân hàng (fakebill, trieustz, tudodevi, gobanklo...), tài xỉu/cờ bạc (banktop, vipclmml), DDoS tools (u696964x, toiyeuni), clone/crack tools
- Báo cáo đã gửi đến Telegram 5201295658

## 02:06–03:00 AM — idle

## 04:04 AM — Dream #10 hoàn tất (auto-dream cron)

- Snapshot BEFORE: 636 lines, 227 entries | AFTER: 662 lines, 249 entries (+22)
- Highlights: 3 template OS mới (AlmaLinux 8.4, CentOS 7.8, Ubuntu 18.04), backup ca.loadip.com TẮT, license Virtualizor hết hạn 31/12/2025, scan vi phạm lần 6 (~350 accounts), 9Router v0.3.84 cài Bot1
- Insight: host .99 timeout 6 lần liên tiếp lúc 2:00 AM chính xác → nghi cron OS/backup job gây spike, cần check /var/log/cron
- Stale threads: platium.ezhost.vn (15 ngày), ShopTui block_qt (10 ngày), host .99 timeout (7 ngày/6 lần)
- Nhắc: Codex token aulax432@gmail.com hết hạn 20/04/2026 (còn 9 ngày)

## 04:05–05:00 AM — idle

## 08:31–08:55 AM — Kẻ bảng + check 9Router + thêm vào OpenClaw

- @sph2vn yêu cầu kẻ bảng 2 account Codex (monglanquachlan1448, yencamtrancam3542)
- Check 9Router: 2 account mới + 2 proxy mới đã có, tất cả active
- Thêm provider `9router` vào openclaw.json: baseUrl `http://163.61.110.178:20128/v1`, models: gpt-4o, gpt-4o-mini, o4-mini
- Backup config: `/root/.openclaw/openclaw.json.bk`
- Primary model vẫn giữ claudible

## 08:55 AM — Báo cáo backup Virtualizor (cron tự động)

- Tổng VPS: 352 (active 342, suspended 10)
- Backup 3 ngày: 314 VPS
- Đang chạy: 0 task, stuck >6h: 7 task
- Lỗi nổi bật: vps28836 (taivps) lỗi 3 lần liên tiếp (mất file trên hypervisor), vps5871 upload INF lỗi, vps5894 integrity check failed
- Đã gửi báo cáo vào Telegram 6028278329

## 09:01 AM — OpenClaw update check (cron)

- Đang chạy: v2026.4.9 — npm mới nhất cũng là 2026.4.9 → không cần update
- Git có nhiều commit mới trên origin/main chưa release: fix session reset, Telegram DM auth, Microsoft Foundry provider, anthropic-vertex, context engine, memory plugins... — chưa có npm release

## 09:01–10:00 AM — idle

- Không có tác vụ mới sau lần check update lúc 09:01

## 10:05–10:06 AM — Dọn #recycle trên NAS backup

- Theo xác nhận của @sph2vn: nếu backup active bên ngoài đã đủ thì dọn `#recycle`
- SSH vào NAS 36.50.134.88:2222, dò lại đúng path thực tế `/volume1/backup/#recycle` (ghi chú cũ `/Backup-vps` không đúng trên máy hiện tại)
- Xóa toàn bộ `backup_host` và `backup_vps` trong `#recycle`
- Dung lượng thu hồi thực tế: từ `11T used / 29T total` xuống `9.7T used / 29T total` → thu hồi khoảng **1.3T**, available tăng lên **20T**
- `#recycle` còn 4.0K sau khi dọn

## 10:58 AM — Đổi Bot 2 sang 9Router làm model mặc định

- Theo yêu cầu của @sph2vn, sửa Bot 2 `160.250.247.29` để dùng `ninerouter/cx/gpt-5.3-codex` làm `agents.defaults.model.primary`
- Tạo backup config trước khi sửa: `/root/.openclaw/openclaw.json.bk1`
- Restart service `openclaw-gateway` sau khi đổi config
- Verify sau restart: gateway active, primary model = `ninerouter/cx/gpt-5.3-codex`

## 11:00 AM — Kiểm tra host .99 có web tài xỉu/cờ bạc

- SSH vào host `160.191.244.99` quét targeted theo tên file + content keywords (`taixiu`, `xocdia`, `sicbo`, `casino`, `nhacai`...) vì cron scan đêm hay timeout ở host này
- Có 2 lần quét giữa chừng bị SIGKILL/timeout khi scan diện rộng → chuyển sang cách xác minh targeted theo account/domain để ra kết quả chắc hơn
- Kết quả xác minh có **ít nhất 4 account nghi vấn rõ ràng** liên quan web tài xỉu/cờ bạc:
  - `kingtele` — domain `kingtele.sbs` — owner `root` — có nhiều file rõ ràng: `public/admin/taixiu.php`, `public/client/taixiu.php`, `ajax/taixiu.php`
  - `hoangphuc68` — domain `hoangphuc68.site` — owner `vuadatac` — có `api/sicbo_cache.json`, `api/api68sic.php`, `api/apihitclub.php`
  - `hoangdungtx6` — domain `hoangdungtx68.site` — owner `vuadatac` — có `xocdia88.html`, `apisun.php`
  - `hack2355` — domain `hackgamevn.site` — owner `taivpsco` — có `assets/xocdia88.jpg`, thêm hit nội dung trong `.trash/api_proxy.php`
- Ngoài ra `thuehacktool` (`thuehacktool.shop`, owner `vuadatac`) có thêm dấu vết rõ hơn trong log/webalizer của các subdomain `toolstaixiu.pro.thuehacktool.shop` và `nhacailuadao.blog.thuehacktool.shop` → rất đáng nghi, cần soi sâu thêm vhost/subdomain nếu muốn chốt 100%

## 11:35 AM — Restart Bot 2 gateway

- Theo yêu cầu của @sph2vn, restart `openclaw-gateway` trên Bot 2 `160.250.247.29`
- Verify sau restart: service active/running, PID mới `1878352`, không thấy lỗi ngay sau khi lên lại

## 11:51–12:00 PM — Hỗ trợ nhanh Telegram

- Giải thích ngắn sự khác nhau giữa `high` và `xhigh` (mức suy luận/speed/cost)
- Trả lời trạng thái sẵn sàng hỗ trợ; chưa phát sinh thao tác hạ tầng mới

## 12:09 PM — Benchmark nhanh models 9Router (Bot 2)

- Theo yêu cầu @sph2vn, chạy benchmark trực tiếp qua endpoint `http://163.61.110.178:20128/v1/chat/completions` với API key Bot 2
- Test toàn bộ **19 model** trong provider `ninerouter` của `/root/.openclaw/openclaw.json`
- Kết quả tổng quan:
  - **Pass 15/19** model Codex (`cx/*`) trả `200 OK`
  - **Fail 4/19**:
    - `gpt-4o`, `gpt-4o-mini`, `o4-mini` → `404 model_not_found` (`No active credentials for provider: openai`)
    - `cx/gpt-5.3-codex-spark` → `400` (model không hỗ trợ cho Codex với ChatGPT account hiện tại)
- Latency nhanh nhất nhóm pass: `cx/gpt-5.3-codex-low` ~`0.845s`; đa số model còn lại ~`1.1–2.6s`
- Lưu ý benchmark dùng prompt ngắn (`Reply exactly: OK`) nên chỉ phản ánh khả năng gọi model + độ trễ cơ bản, chưa phải quality benchmark sâu

## 12:23 PM — Benchmark suy luận code (Codex variants)

- Chạy mini benchmark 4 bài reasoning code Python (`fib7`, `slice_sum`, `alias_dict`, `collatz4`) trên 6 model: `cx/gpt-5.4`, `cx/gpt-5.3-codex`, `low/high/xhigh/none`
- Kết quả accuracy:
  - `cx/gpt-5.3-codex-low`: **4/4 (100%)** — avg ~`1.99s`
  - `cx/gpt-5.3-codex`: **4/4 (100%)** — avg ~`2.149s`
  - `cx/gpt-5.3-codex-high`: **4/4 (100%)** — avg ~`2.872s`
  - `cx/gpt-5.4`: **4/4 (100%)** — avg ~`3.437s`
  - `cx/gpt-5.3-codex-xhigh`: **4/4 (100%)** — avg ~`4.465s`
  - `cx/gpt-5.3-codex-none`: **2/4 (50%)** — avg ~`1.501s`
- Nhận định nhanh: mode `none` nhanh hơn nhưng giảm độ chính xác; `low` đang cho tỉ lệ/độ trễ tốt nhất trong bộ test ngắn này

## 12:42–13:00 PM — Benchmark nặng suy luận code (20 bài)

- Theo yêu cầu @sph2vn, chạy benchmark nặng `20 tasks × 6 models = 120 calls` qua script `/root/bench_code_heavy.py` trên Bot 2
- Kết quả tổng hợp (`/root/bench_code_heavy_result.json`):
  - `cx/gpt-5.3-codex-low`: **20/20 (100%)**, avg `3.248s`, p95 `8.123s`
  - `cx/gpt-5.3-codex-high`: **20/20 (100%)**, avg `3.509s`, p95 `5.097s`
  - `cx/gpt-5.3-codex`: **20/20 (100%)**, avg `3.513s`, p95 `5.637s`
  - `cx/gpt-5.4`: **20/20 (100%)**, avg `3.654s`, p95 `5.494s`
  - `cx/gpt-5.3-codex-xhigh`: **20/20 (100%)**, avg `4.648s`, p95 `7.712s`
  - `cx/gpt-5.3-codex-none`: **16/20 (80%)**, avg `1.242s`, p95 `4.835s`
- Kết luận: nhóm `low/high/default/5.4/xhigh` đều giữ độ đúng 100% trên bộ 20 bài; `none` nhanh nhất nhưng giảm accuracy rõ rệt

## 13:00–13:01 PM — Tổng kết giờ

- Đã ghi đầy đủ các tác vụ 12:00–13:00 vào nhật ký (benchmark model + benchmark code mini/nặng)
- Không phát sinh thao tác hạ tầng mới ngoài các mục đã ghi

## 13:13–14:00 PM — Tư vấn nhanh model Codex

- Trả lời chuỗi câu hỏi của @sph2vn về khác biệt giữa `cx/gpt-5.3-codex`, `...-none`, `...-low/high/xhigh`
- Xác nhận qua ảnh: bot đang chọn `ninerouter/cx/gpt-5.3-codex-none`
- Khuyến nghị vận hành:
  - Tránh `none` cho tác vụ code quan trọng (accuracy thấp hơn)
  - Dùng `codex` hoặc `codex-low` làm mặc định; case khó chuyển `high`
- Không thay đổi config hay restart thêm trong khoảng thời gian này

## 14:05 PM — Check backup VPS IP 160.191.244.160 trên Virtualizor master

- SSH vào master `160.191.245.166`, query DB Virtualizor (`virtualizor`) để xác minh trực tiếp
- IP `160.191.244.160` map tới `vpsid=5733` (`hostname=Host`, `vps_name=v3678`, `serid=0`, `bpid=2421`)
- Bảng `backups`: **không có bản ghi backup nào cho `vpsid=5733`**
- Process hiện tại: không thấy job backup chạy riêng cho `vpsid=5733`; chỉ thấy các job backup plan khác (`2418`)
- Log backup plan tại `/var/virtualizor/log/backup_plan/`: chỉ có file `2418-*`, **không có `2421-*`**
- Kết luận tại thời điểm check: VPS này **không đang backup** và có dấu hiệu **chưa được chạy backup theo plan 2421** (chưa thấy log/record backup)

## 14:22 PM — Re-check sau khi anh chạy lại backup

- Đã xuất hiện plan log mới: `/var/virtualizor/log/backup_plan/2421-22305.log` (plan `2421` = `Backup-HOST`)
- DB vẫn chưa có record mới trong `backups` cho `vpsid=5733`
- Process đang chạy:
  - `/usr/local/virtualizor/scripts/vps_backup_master.php a=22305`
  - `/usr/local/virtualizor/scripts/vpsbackups_plan.php -b2421 -m22305`
- Nội dung log `2421-22305.log` chỉ lặp `Sleeping for 5 seconds...`, chưa vào bước `Starting backup of VPS 5733`
- Nhận định: job **đã được trigger** nhưng đang **kẹt chờ** (queue/lock bởi các backup process cũ còn treo), chưa thực sự backup VPS 5733

## 14:44 PM — Kill backup process cũ để gỡ kẹt queue

- Theo yêu cầu @sph2vn, kill các process backup cũ treo lâu (13 PID), giữ lại job hiện tại `a=22305` / `-b2421 -m22305`
- Trước khi kill: có nhiều process backup cũ tồn tại từ các actid `20038/20704/21074/21662/22285...`
- Sau khi kill: chỉ còn 2 process của job hiện tại (`a=22305` và `-b2421 -m22305`)
- Tuy nhiên ngay sau kill, log `2421-22305.log` vẫn còn lặp `Sleeping for 5 seconds...` (chưa chuyển sang `Starting backup of VPS 5733`)

## 14:46–15:00 PM — Theo dõi chờ nhắc check lại

- Xác nhận với @sph2vn tạm dừng theo dõi liên tục, chờ anh nhắc rồi check tiếp
- Không phát sinh thao tác hạ tầng mới trong khoảng này

## 15:13–16:00 PM — Trao đổi vận hành context + cấu hình bootstrap

- Giải thích cảnh báo context/session token gần ngưỡng 128k và hiện tượng `MEMORY.md` bị truncate
- Theo yêu cầu @sph2vn, tăng giới hạn bootstrap qua gateway config:
  - `agents.defaults.bootstrapMaxChars = 1000000`
  - `agents.defaults.bootstrapTotalMaxChars = 1000000`
- Tư vấn trade-off: context lớn hơn có thể tăng độ trễ + tăng token/cost
- Trả lời nhanh năng lực pentest: có thể hỗ trợ pentest authorized theo hướng an toàn, không phá dịch vụ

## 16:30 PM — Kill job backup treo của VPS 5733 (.160)

- Theo yêu cầu @sph2vn, kill 2 PID job treo:
  - `3943164` (`vps_backup_master.php a=22305`)
  - `3999877` (`vpsbackups_plan.php -v5733 -a22306 -m22305`)
- Verify sau kill: không còn thấy 2 process này trong `ps`

## 16:32 PM — Re-run backup báo lỗi, bắt được root cause

- Lần chạy mới tạo log `/var/virtualizor/log/backup_plan/2421-22307.log` và task `Actid 22308`
- Log lỗi tại `/var/virtualizor/log/backup/5733-A22308-M22307.log`:
  - QEMU trả về: `Node '#block526' is busy: block device is in use by block job: backup`
  - Kết thúc: `Backup Process completed UNSUCCESSFULLY`
- Kiểm tra trực tiếp trạng thái block job VM `v3678`:
  - `virsh blockjob v3678 vda --info` báo không thấy ở `vda`
  - `virsh qemu-monitor-command v3678 '{"execute":"query-jobs"}'` vẫn thấy job `id=virt_backup_5733`, `type=backup`, `status=running`
- Kết luận: backup fail do **block-job QEMU cũ còn treo (`virt_backup_5733`)**; cần cancel job này rồi chạy backup lại

## 16:36 PM — Gỡ sạch block-job treo theo yêu cầu

- Chạy `job-cancel` cho job QEMU `virt_backup_5733` trên VM `v3678`
- Trước khi cancel: `query-jobs` còn 1 job backup đang chạy
- Sau khi cancel: `query-jobs` trả về rỗng `[]` (đã sạch)
- Không còn process backup Virtualizor liên quan đang chạy

## 16:41 PM — Check sau khi chạy lại backup (thành công bước start)

- Có plan log mới: `/var/virtualizor/log/backup_plan/2421-22309.log`
  - `Starting backup of VPS 5733 with Actid 22310`
  - `All Backup scripts started successfully`
- Process hiện đang chạy bình thường:
  - `vps_backup_master.php a=22309`
  - `vpsbackups_plan.php -v5733 -a22310 -m22309`
- Log worker `/var/virtualizor/log/backup/5733-A22310-M22309.log` cho thấy `drive-backup` trả `{"return":{}}` (không lỗi immediate)
- `query-jobs` trên VM `v3678` đang có `virt_backup_5733 status=running` với progress tăng (backup đang chạy thực tế)
- Tại thời điểm check chưa có row mới trong bảng `backups` cho `vpsid=5733` (thường ghi sau khi hoàn tất)

## 16:53 PM — Điều tra vì sao backup báo ~622GB cho host .160

- Check trực tiếp trong guest `160.191.244.160`:
  - `df -h /` => dùng **241GB / 985GB** (26%)
  - `df -ih /` => inode dùng **15%** (không có dấu hiệu “quá nhiều file nhỏ”)
  - `du -x --max-depth=1 /var` ~ **41GB** (không bất thường lớn)
- Check trên hypervisor (VM `v3678`):
  - File qcow2 `/nvme2/v3678-...qcow2` logical 964GB, allocated thực tế **~685GB**
- Nhận định: số backup ~622GB xuất phát từ **block đã allocate trong qcow2** (bao gồm dữ liệu cũ đã xóa nhưng chưa reclaim), **không phải** guest hiện đang dùng 622GB

## 16:55–17:00 PM — Hậu kiểm

- Có nhiều lệnh `du` quét rộng bị SIGKILL do timeout/tài nguyên; kết quả hữu ích vẫn giữ từ các check thành công (`df`, `df -ih`, `du /var`, qcow2 size trên hypervisor)
- Heartbeat 16:59: đọc đúng `HEARTBEAT.md` (comment-only), phản hồi `HEARTBEAT_OK`

## 17:04 PM — Đối chiếu số liệu df .160/.99

- Re-check SSH trực tiếp 2 host bằng cả 2 đơn vị:
  - `.160`: `df -h /` => 985G used 241G; `df -H /` => 1.1T used 259G
  - `.99`: `df -h /` => 1.8T used 620G; `df -H /` => 2.0T used 666G
- Kết luận: không lệch dữ liệu; khác nhau do **đơn vị nhị phân (`-h`, GiB/TiB)** vs **thập phân (`-H`, GB/TB)**

## 17:07 PM — Check vì sao NAS chưa thấy file backup .160

- Job backup hiện tại vẫn đang chạy:
  - `vps_backup_master.php a=22309`
  - `vpsbackups_plan.php -v5733 -a22310 -m22309`
- `query-jobs` trên VM `v3678`: `virt_backup_5733 status=running`, progress `173822115840 / 1073741824000` (~16%)
- File tạm đang ghi local trên hypervisor: `/nvme2/backup-5733-0/5733_Host_2026_04_11-16_40_07.qcow2` (~46G tại thời điểm check)
- Bảng `backups` chưa có record mới cho `vpsid=5733`; NAS chưa thấy file vì chưa đến bước hoàn tất + chuyển/ghi record

## 17:31 PM — Re-check tiến độ backup VPS 5733

- Process vẫn chạy:
  - `vps_backup_master.php a=22309` (elapsed ~52m)
  - `vpsbackups_plan.php -v5733 -a22310 -m22309` (elapsed ~52m)
- Tiến độ QEMU backup: `337950670848 / 1073741824000` (~31.5%)
- File tạm local đã tăng lên ~`89G`: `/nvme2/backup-5733-0/5733_Host_2026_04_11-16_40_07.qcow2`

## 17:55 PM — Re-check tiến độ lần tiếp theo

- Process vẫn chạy ổn:
  - `a=22309` elapsed ~1h15m
  - `a22310` elapsed ~1h15m
- Tiến độ QEMU backup: `489669394432 / 1073741824000` (~45.6%)
- File tạm local: ~`137G`
- DB `backups` vẫn chưa có row mới cho `vpsid=5733` (chưa hoàn tất)

## 17:00–18:00 PM — Tổng kết giờ

- Theo dõi liên tục job backup VPS `.160` (`vpsid=5733`, act `22309/22310`) sau khi gỡ block-job treo
- Nhiều lần check tiến độ qua `query-jobs` + kích thước file tạm local `/nvme2/backup-5733-0/5733_Host_2026_04_11-16_40_07.qcow2`
- Xác nhận cho @sph2vn rằng NAS chưa thấy file final vì job chưa complete; hiện backup vẫn chạy bình thường

## 18:20 PM — Re-check tiến độ backup

- Process vẫn sống (`a=22309`, `a22310`) elapsed ~1h40m
- QEMU progress: `729509068800 / 1073741824000` (~67.9%)
- File tạm local: ~`173G`
- DB `backups` chưa có row mới cho `vpsid=5733`

## 18:46 PM — Re-check tiến độ backup

- Process vẫn chạy (`a=22309`, `a22310`) elapsed ~2h07m
- QEMU progress: `938505142272 / 1073741824000` (~87.4%)
- File tạm local: ~`203G`
- Chưa có row mới trong DB `backups` (chưa finalize)

## 18:00–19:00 PM — Tổng kết giờ

- Theo dõi tiếp tiến độ backup VPS `.160` (`vpsid=5733`) theo yêu cầu @sph2vn
- Cập nhật realtime nhiều lần: job vẫn chạy ổn, tiến độ tăng từ ~68% lên ~87.4%
- Xác nhận chưa có bản ghi `backups` mới vì chưa hoàn tất phase finalize/push NAS

## 19:32 PM — Query WHMCS: VPS gia hạn <30k/tháng

- Truy vấn trực tiếp DB WHMCS `sql_my_h2cloud_v` trên `36.50.135.120:2000`
- Scope: dịch vụ `Active`, `servertype IN ('virtualizor','h2cloud')`, quy đổi về giá tháng theo billing cycle
- Kết quả: **36 VPS** có giá quy đổi gia hạn dưới **30,000đ/tháng**
- Breakdown: `Monthly 28`, `Annually 6`, `Quarterly 1`, `Semi-Annually 1`
- Đã xuất list IP (field `dedicatedip`) cho 36 dịch vụ để gửi @sph2vn

## 19:51 PM — Query bổ sung WHMCS theo yêu cầu

- VPS `Active` có giá quy đổi dưới **40,000đ/tháng**: **42** dịch vụ
- Check riêng gói có tên chứa `4-8`, billing `Annually`, giá năm `<800,000đ` (scope `Active`, servertype VPS): **không có dịch vụ nào**
- Check thêm gói tên chứa `4-10` (mọi trạng thái): **không có dịch vụ nào**
- Query `Annually < 400,000đ` (scope `Active`, servertype VPS): **6 dịch vụ**
  - 3 dịch vụ `Plan 1-2-25` giá 110,000đ/năm
  - 1 dịch vụ `Plan 1-2-25` giá 200,000đ/năm
  - 2 dịch vụ `Plan 1-1-20` giá 240,000đ/năm
- So giá theo mốc anh đưa (`4-6=150k/tháng`, `2-2=66k/tháng`, `4-8=980k/năm`):
  - `2-2`: có nhiều service đang **60k/tháng** và nhóm **500k/năm** (~41.7k/tháng) + 1 service free (bất thường rẻ)
  - `4-6`: có 2 service `Plan 4-6-40` đang **980k/năm** (~81.7k/tháng), thấp hơn mốc 150k/tháng
  - `4-8`: đa số 978k/980k/999k năm (gần chuẩn), không thấy dưới 800k

## 19:00–20:00 PM — Tổng kết giờ

- Theo dõi tiến độ backup VPS `.160` (`vpsid=5733`) đến ~87.4% lúc 18:46
- Thực hiện nhiều truy vấn WHMCS theo yêu cầu @sph2vn:
  - Đếm VPS dưới 30k/tháng (36), dưới 40k/tháng (42)
  - Xuất list IP + giá các dịch vụ dưới 30k
  - Check gói `4-8` năm dưới 800k: không có
  - Check gói `4-10`: không có
  - Check dịch vụ năm dưới 400k: 6 dịch vụ
  - Đối chiếu giá bất thường theo baseline 4-6/2-2/4-8 và chỉ ra nhóm lệch giá

## 20:08 PM — Check kết quả backup VPS 5733 (lần chạy 22309/22310)

- `query-jobs` trả rỗng, process backup đã kết thúc
- Log `/var/virtualizor/log/backup/5733-A22310-M22309.log` kết luận:
  - `VPS Backup Completed`
  - `Uploading to backup server`
  - **ERROR:** `Unable to connect the server via SSH`
  - `ssh2_auth_password() expects parameter 1 to be resource, bool given`
  - `Backup Process completed UNSUCCESSFULLY`
- Test SSH thủ công từ master `.166` sang NAS `36.50.134.88:2222` bằng `sysadmin/Hoangdz123@` vẫn đăng nhập được
- Soi config backup server `bid=19`:
  - `hostname=Backup-SSH` (không resolve DNS)
  - `internal_hostname=36.50.134.88` (IP đúng)
- Verify:
  - `getent hosts Backup-SSH` => `NO_DNS`
  - test `ssh2_connect('36.50.134.88',2222)` + `ssh2_auth_password(...)` bằng `/usr/local/emps/bin/php` => `connect_ok`, `true`
- Nhận định: fail do Virtualizor dùng `hostname=Backup-SSH` không resolve được, nên `ssh2_connect` trả `false` trước bước auth; không phải do NAS down

<!-- consolidated -->
